---
// server-side code goes here and can be accessed below
import AstraButton from '../components/button'
import AstraCard from '../components/card'
import AstraInput from '../components/input'
import AstraLabel from '../components/label'
import AstraSelect from '../components/select'

import '@fontsource/inter' // Defaults to weight 400

const optionsArray = [
    { label: 'Apples', value: 'apples' },
    { label: 'Oranges', value: 'oranges' },
    { label: 'Avocados', value: 'avocados' },
]
---

<html lang="en" class="h-full">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />
        <style>
            body {
                margin: 2rem;
            }
            h2:not(:first-child) {
                margin-top: 2rem;
                border-top: 1px solid lime;
            }
        </style>
        <script>
            document.getElementById('arbitrary-button-id')?.addEventListener('click', () => alert('👏🎉'))
        </script>
    </head>
    <body>
        <main>
            <h2><pre>&lt;astra-input /&gt;</pre></h2>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraInput client:only="lit" />

            <h2><pre>&lt;astra-button /&gt;</pre></h2>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraButton client:only="lit" id="arbitrary-button-id">click click click click click</AstraButton>

            <h2><pre>&lt;astra-card /&gt;</pre></h2>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraCard client:only="lit" id="arbitrary-button-id">
                <div>Div tag</div>
                <p>Paragraph tag</p>
                <span>Span tag</span>
            </AstraCard>

            <h2><pre>&lt;astra-label /&gt;</pre></h2>
            <AstraLabel client:only="lit">variant=undefined</AstraLabel>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraLabel client:only="lit" variant="label">variant=label</AstraLabel>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraLabel client:only="lit" variant="h1">variant=h1</AstraLabel>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraLabel client:only="lit" variant="h2">variant=h2</AstraLabel>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraLabel client:only="lit" variant="h3">variant=h3</AstraLabel>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraLabel client:only="lit" variant="h4">variant=h4</AstraLabel>

            <h2><pre>&lt;astra-select /&gt;</pre></h2>
            <!-- @ts-ignore Astro doesn't seem to understand some of the params being passed -->
            <AstraSelect client:only="lit" options={optionsArray} placeholder="Choose a fruit" />
        </main>
    </body>
</html>
